name: dynamic_report_app
description: "A dynamic report application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  flutter_bloc: ^8.1.3
  json_annotation: ^4.8.1
  equatable: ^2.0.7
  pluto_grid: ^8.0.0
  share_plus: ^11.0.0
  path_provider: ^2.1.5
  excel: ^4.0.6
  go_router: ^15.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
